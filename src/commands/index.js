import renderDay from './renderDay';
import renderMonth from './renderMonth';

export default {
  update: () => {
    console.log(
      'This feature is planned at the end of the year in the next version, please be patient.',
    );
  },
  renderDay,
  renderMonth,
};
